const { MongoClient } = require('mongodb');
const u = require('./u');

(async () => {
  const mdb = {
    host: '127.0.0.1',
    user: 'synopsis',
    pass: 'synopsis',
    name: 'reference_urls',
  };
  
})();
